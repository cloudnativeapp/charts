image:
  repository: registry.cn-hangzhou.aliyuncs.com/kubeapps/gcr-authenticator
  tag: v0.1.0
  pullPolicy: IfNotPresent

config: {}

resources: {}

hostPathConfig:
  output: /srv/kubernetes/aws-iam-authenticator/
  state: /srv/kubernetes/aws-iam-authenticator/
