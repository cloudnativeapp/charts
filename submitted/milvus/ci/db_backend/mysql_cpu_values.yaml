cache:
  insertBufferSize: 4
  cpuCacheCapacity: 8

gpu:
  enabled: false

mysql:
  enabled: true
  persistence:
    enabled: false
    storageClass: default
    accessMode: ReadWriteOnce
    size: 5Gi
